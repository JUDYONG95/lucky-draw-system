# Allow sharp to run its postinstall build script (required for Next.js image optimization)
side-effects-cache=true

# pnpm v9+ and v11+ setting to allow sharp build scripts
only-built-dependencies[]=sharp
